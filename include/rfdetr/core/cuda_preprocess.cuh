#pragma once
// ImagePreprocessor / launch_square_resize_normalize live in src/internal/.
// Shim for backward compatibility with apps/ and tools.
#include "../../../src/internal/cuda_preprocess.cuh"
